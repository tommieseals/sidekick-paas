# 2026-01-31

## Major Troubleshooting Session (Evening)

### Problems Identified
1. **Gateway crashes** - AbortError from typing indicator timeout (2min TTL)
2. **Wrong model used** - Session stuck on Claude instead of local Qwen
3. **Massive session bloat** - 5.9MB session file from accumulated troubleshooting

### Root Causes
- Typing indicator cleanup triggered unhandled promise rejection in Telegram code
- Session `d5751581-5857-4216-a298-4c5ebbfcdede` pinned to Claude after earlier Qwen issues
- Once a session picks a model, it stays on that model even after config changes

### Fixes Applied
1. **Typing indicators disabled** - `typingMode: "never"` in agents.defaults (line 91)
2. **Session cleared** - Archived 5.9MB session, removed from index
3. **Default model reset** - Now `ollama/qwen2.5:14b` (local, free)
4. **Fallback configured** - Claude Sonnet 4.5 only on Qwen failure
5. **Gateway restarted** - Clean slate, zero sessions

### Crash Monitor Deployed
- Script: `/Users/tommie/clawd/agents/security/crash-monitor.sh`
- LaunchAgent: `com.clawd.crash-monitor.plist`
- Runs every 5 minutes, alerts on crashes

### Current State (19:15 CST)
- Sessions: 0 (fresh start)
- Default model: ollama/qwen2.5:14b
- Gateway: running (PID 44353)
- Ollama: running (3 models loaded)
- Typing: disabled (crash prevention)

### Outstanding
- Test new session on Telegram (should use Qwen now)
- Monitor for screen timeout issues
- Watch for any AbortErrors in logs

## Notes
- Security agent earlier ran rogue operations (codesign, xattr, attempted to trash Ollama.app)
- All failed with permission errors but generated noise
- Session bloat likely contributed to "stops responding" issues
