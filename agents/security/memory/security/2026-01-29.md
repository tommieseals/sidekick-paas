# Security Scan Report — 2026-01-29

**Scan time:** ~15:46 CST  
**Host:** Tommie's Mac mini (Darwin 24.6.0, arm64)

---

## 1. Firewall Status ⚠️ WARNING

- **macOS Application Firewall is DISABLED** (State = 0)
- Recommendation: Enable via System Settings → Network → Firewall, or run:
  ```
  sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate on
  ```

## 2. Open Ports

| Port | Process | Scope | Notes |
|------|---------|-------|-------|
| 5000 | ControlCenter | All interfaces (IPv4+IPv6) | AirPlay Receiver |
| 7000 | ControlCenter | All interfaces (IPv4+IPv6) | AirPlay Receiver |
| 7070 | **AnyDesk** | All interfaces (IPv4+IPv6) | ⚠️ Remote desktop — exposed on all interfaces |
| 49185 | rapportd | All interfaces (IPv4+IPv6) | Apple Rapport (Nearby devices) |
| 18789 | node (Clawdbot) | localhost only | Gateway |
| 18791 | node (Clawdbot) | localhost only | Gateway sub-service |
| 18792 | node (Clawdbot) | localhost only | Gateway sub-service |

### Observations
- **AnyDesk on port 7070** is listening on all interfaces. If not actively used, consider disabling or restricting it.
- AirPlay ports (5000, 7000) are standard but exposed on all interfaces.
- Clawdbot node services are properly bound to localhost only ✅

## 3. Suspicious Processes

- **No processes running from /tmp, /var/tmp, or other unusual locations** ✅
- Chrome Remote Desktop host detected (`remoting_me2me_host`) using a temp socket at `/tmp/chromoting.tommie.ssh_auth_sock` — this is normal CRD behavior.
- Root processes are all standard macOS system daemons ✅
- **Two remote access tools active:** AnyDesk + Chrome Remote Desktop. Confirm both are intentional.

## 4. Failed Login Attempts

- **No failed SSH login attempts** found in last 24h ✅
- **No suspicious authentication failures** detected in system logs ✅
- Recent sessions are all user `tommie` via local TTY — looks normal.

## 5. macOS Software Updates ⚠️ WARNING

**3 updates available:**

| Update | Version | Size | Action |
|--------|---------|------|--------|
| Safari | 26.2 | ~220 MB | No restart |
| **macOS Sequoia** | **15.7.3** | **~2.1 GB** | **Restart required** |
| macOS Tahoe | 26.2 | ~7.7 GB | Restart required |

- **Sequoia 15.7.3** is a recommended security patch — should be applied soon.
- Safari 26.2 is a quick update with no restart needed.
- macOS Tahoe 26.2 is a major version upgrade (optional).

## 6. Sensitive File Permissions

### ~/.ssh ✅
- Directory: `drwx------` (700) — **Correct**, owner-only access
- `known_hosts`: `rw-r--r--` (644) — acceptable (no secrets)
- No private keys present (only `known_hosts`)

### ~/.clawdbot ✅
- Directory: `drwx------` (700) — **Correct**, owner-only access
- Config files (`clawdbot.json`, backups): `rw-------` (600) ✅
- Credentials directory: `drwx------` (700) ✅
- Subagents/telegram dirs: `drwx------` (700) ✅
- **Minor note:** `logs/` directory is `drwxr--r--` (744) — slightly more open than needed but contains non-sensitive data.

---

## Summary

| Category | Status | Priority |
|----------|--------|----------|
| Firewall | ⚠️ DISABLED | **HIGH** — Enable ASAP |
| Open ports | ⚠️ AnyDesk exposed | **MEDIUM** — Review if needed |
| Suspicious processes | ✅ Clean | — |
| Failed logins | ✅ None detected | — |
| Software updates | ⚠️ 3 pending | **MEDIUM** — Apply Sequoia 15.7.3 |
| File permissions | ✅ Good | — |

### Top Recommendations
1. **Enable the macOS firewall** — this is the most important finding
2. **Review AnyDesk** — if not actively needed, disable it; if needed, ensure it has a strong password
3. **Apply macOS Sequoia 15.7.3** security update at next convenient restart
4. **Apply Safari 26.2** update (no restart needed)
